import React from "react";

/*
let ItemSearchJob = (props) => {

    return props.job ? props.job.map((item) => {
        const id = item.id;
        const name = item.name;



        return (
            <>

                <div><p>{name}</p><p>{id}</p></div>
                {console.log(name)}
            </>

        )
    }) : []
}

export default ItemSearchJob;*/
